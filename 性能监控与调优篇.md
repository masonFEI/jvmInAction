# 第1章--概述篇

## 性能优化三部曲

1. 性能监控
2. 性能分析
3. 性能调优

## 性能评价/测试指标

1. 停顿时间（或响应时间）
2. 吞吐量
3. 并发数
4. 内存占用
5. 相互间的关系

# 第2章--JVM监控及诊断工具-命令行篇

## jps(java process status):查看正在运行的java进程

- options参数：
    - -q:只有pid
    - -l:显示主类的全路径或jar包的全路径
    - -v:列出虚拟机进程启动时的jvm参数
    - -m:显示传递给主类的参数
- -XX:-UsePerfData:禁用性能数据收集功能，jps命令将无法获取正在运行的Java进程的信息。

## jstat(JVM statistics monitoring tool):监控JVM的内存、垃圾回收、类加载等性能指标

jstat -<option> [-t] [-h<lines>] <vmid> [<interval> [<count>]]

jstat -class -t -h3 23752 1000 10

- options参数：
    - 类加载相关的：
        - -class:类加载统计信息
    - 垃圾回收相关的：
        - -gc:垃圾回收统计信息
        - -gccapacity:垃圾回收内存容量统计信息
        - -gcutil:垃圾回收内存使用率统计信息
        - -gccause:垃圾回收原因统计信息
        - -gcnew:新生代垃圾回收统计信息
        - -gcnewcapacity:新生代垃圾回收内存容量统计信息
        - -gcnewutil:新生代垃圾回收内存使用率统计信息
        - -gcold:老年代垃圾回收统计信息
        - -gcoldcapacity:老年代垃圾回收内存容量统计信息
    - JIT相关的：
        - -compiler:编译器统计信息
        - -printcompilation:输出已经被JIT编译的方法信息
- interval参数：监控数据的采样间隔，单位为毫秒
- count参数：采样的次数，默认为无限次
- -t参数：显示时间戳,单位为秒
- -h参数：指定显示的行数，默认为20行

## jinfo(Configuration Info for Java):查看和修改正在运行的Java进程的配置信息

jinfo -<option> <vmid>

options参数：

- -sysprops:显示系统属性信息
- -flags:显示JVM的标志信息
- -flag <name>:显示指定标志的信息
- -flag [+|-]<name>:启用或禁用指定的标志
- -diag:显示诊断命令信息

并非所有的参数都支持动态修改，参数只有被标记为manageable的才可以动态修改

可以查看被标记为manageable的参数列表：
java -XX:+PrintFlagsFinal -version | grep manageable

java -XX:+PrintFlagsInitial: 查看所有JVM参数的初始值
java -XX:+PrintFlagsFinal: 查看所有JVM参数的最终值（包括被修改过的参数）
java -XX:PrintCommandLineFlags: 查看JVM参数的命令行选项

## jmap(memory map for java):导出内存映像文件&内存使用情况

获取dump文件（二进制文件）；还可以获取目标java进程的内存使用情况（文本信息）
jmap导出的堆快照必定是安全点位置的快照；如果某个线程长时间无法跑到安全点，jmap将一直等下去。

基本使用语法：

- jmap [option] <vmid>

options参数：

- -dump:生成heap dump文件
    - format=b,file=<filename>:指定dump文件的格式和名称
    - live:只包含存活对象的dump文件
- -heap:显示堆的内存使用情况和垃圾回收器信息
- -histo:显示堆中对象的统计信息
    - live:只包含存活对象的统计信息
- -permstat:显示永久代（或元空间）的内存使用情况和类加载信息
- -finalizerinfo:显示等待被finalize方法回收的对象信息(仅linux/solaris系统支持)
- -F:强制生成dump文件，即使目标进程没有响应
- -clstats:显示类加载统计信息

### 导出内存映像文件

- 手动导出：
    - jmap -dump:format=b,file=heap_dump.hprof <pid>
    - jmap -dump:live,format=b,file=heap_dump.hprof <pid>:只包含存活对象的dump文件
- 通过JVM参数自动导出：-XX:+HeapDumpOnOutOfMemoryError:当发生OOM错误时自动生成heap dump文件
- 通过JVM参数自动导出并指定文件路径：-XX:HeapDumpPath=/path/to/dump:当发生OOM错误时自动生成heap dump文件，并将文件保存到指定路径

## jhat(Java Heap Analysis Tool):分析heap dump文件

jhat [options] <dump file>

## jstack(Java Stack Trace):查看正在运行的Java进程的线程堆栈信息

jstack [option] <vmid>

options参数：

- -l:显示锁信息
- -m:显示本地方法栈信息
- -F:强制生成线程堆栈信息，即使目标进程没有响应

## jcmd(Java Diagnostic Command):执行诊断命令

jcmd <vmid> <command> [options]
jcmd 拥有jmap的大部分功能，并且还可以执行其他诊断命令，如gc、thread、vm等命令。
jcmd -l 列出所有的jvm进程
jcmd pid help 列出所有可用的命令

## jstatd(Java Stack Trace Daemon):远程监控和诊断工具

# 第3章--JVM监控及诊断工具-GUI篇

## jconsole(Java Monitoring and Management Console):监控和管理Java应用程序的图形界面工具

可以检测死锁

## visualvm(Java VisualVM):监控和分析Java应用程序的图形界面工具

主要功能：
1.生成堆快照（dump文件）
2.分析堆快照
3.监控内存使用情况
4.监控CPU使用情况
5.监控线程状态
6.分析垃圾回收日志

## MAT(Memory Analyzer Tool):分析heap dump文件的图形界面工具

hitogram视图（直方图）：显示堆中对象的统计信息，按照类名进行分组，并显示每个类的实例数量和占用内存大小。
支配树视图（Dominator Tree）：显示对象之间的引用关系，帮助分析内存泄漏和对象保留问题。

### 分析堆dump文件

1. 获得对象相互引用的关系:出引用与入引用（with outgoing reference,with incoming reference）
2. 浅堆与深堆
    - 浅堆(Shallow Heap)：是指一个对象本身占用的内存大小，不包括它引用的其他对象占用的内存。
      24字节为String浅堆的大小，与value实际取值无关，与字符串长度无关。
    - 保留集：仅被对象A所持有的对象的集合。
    - 深堆（Retained Heap）：是指对象的保留集中所有的对象的浅堆大小之和；即对象A被回收后，可以释放的真实空间。

### 支配树

支配树体现了对象实例间的支配关系。
在对象引用图中，所有指向对象B的路径都经过对象A,则认为对象A支配对象B。
如果对象A是离对象B最近的一个支配对象，则认为对象A是对象B的直接支配者。

## 内存泄漏的理解和分类

严格来说，只有对象不会再被程序用到了，但是GC又不能回收他们的情况，才叫内存泄漏；
内存泄漏和内存溢出的关系：内存泄漏的增多，最终会导致内存溢出。

### java中内存泄漏的8种情况

1. 静态集合类（如HashMap、ArrayList等）持有对象引用，导致这些对象无法被GC回收。
2. 单例模式中的对象被长时间持有，导致内存泄漏。
3. 内部类持有外部类，导致外部类无法被GC回收。
4. 各种连接，如数据库连接，网络连接和io连接等
5. 变量不合理的作用域
6. 改变哈希值，类的hashCode方法的实现不合理，导致对象无法被正确地存储和回收。
   当我们想把自己定义的类保存到散列表的时候，需要保证对象的hashCode不可变。
7. 缓存泄漏，使用WeakHashMap等弱引用的集合类来存储对象，可以避免缓存泄漏的问题。
8. 监听器和回调

## Jprofiler:性能分析工具

### 两种数据采集方式

1. 采样（Sampling）：通过定期检查线程的状态来收集性能数据，开销较小，但可能会遗漏一些细节。
2. 重构模式（Instrumentation）：通过修改字节码来插入监控代码，能够提供更详细的性能数据，但开销较大。

### 内存视图

分析：内存中的对象的情况

- 频繁创建的java对象：死循环、循环次数过多
- 存在大对象：读取文件时，byte[]应该边读边写--> 如果长时间不写出的话，导致byte[]过大
- 存在内存泄漏

## Arthas:阿里巴巴开源的Java诊断工具

java -jar arthas-boot.jar pid

monitor/trace/stack/tt:观测性能

## JMC(Java Mission Control):监控和分析Java应用程序的图形界面工具

飞行记录器，要采用取样，必须先添加参数(否则会报错)：

- -XX:+UnlockCommercialFeatures
- -XX:+FlightRecorder

# 第4章--JVM运行时参数

- 类型一：标准参数选项
- 类型二：-X参数选项
- 类型三：-XX参数选项

# 第5章--分析GC日志

# 第6章--OOM常见各种场景及解决方案

# 第7章--性能优化案例

# 第8章--Java代码层及其他层面调优