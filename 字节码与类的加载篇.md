# 第1章--Class文件结构

![class文件结构](assets/image/class文件结构.jpg)

## 前端编译器

javac是一种能够将java源码编译为字节码的前端编译器
静态提前编译器（AOT编译器）直接把.java文件编译成本地机器指令

字节码（byte code）:操作码+操作数 / 单独操作码

## class文件结构

class文件的本质：class文件是一组以8位字节为基础单位的二进制数据流。

class文件格式采用一种类似于C语言结构体的方式进行数据存储，这种结构中只有两种数据类型：无符号数和表
无符号数：u1（1字节）、u2（2字节）、u4（4字节）、u8（8字节）；无符号数可以用来描述数字、索引引用、数量值或者按照UTF-8编码构成字符串值
表：一组相关联的数据项的集合；整个Class文件本质上就是一个表，由于表没有固定长度，所有通常会在其前面加上个数说明。

class文件的总体结构如下：

- 魔数：识别class文件的唯一标识，固定值0xCAFEBABE
- Class文件版本：minor_version和major_version
- 常量池:Class文件中内容最为丰富的区域之一（资源仓库），用于存储类和接口的各种字面量和符号引用，是Java运行时数据区中的运行时常量池的静态表示
    - 常量池计数器：u2类型，表示常量池中常量的数量，注意常量池的索引是从1开始的，任何都不引用索引0
    - 常量池表：常量池中的具体内容，由一系列常量池项组成，每个常量池项都有一个标志位，表示该项的类型
        - 描述符的作用：描述符用于唯一标识类、接口、字段和方法的类型信息，方法的参数列表和返回值类型
        - 这14种常量池项类型：
            - CONSTANT_Class_info（类或接口的符号引用）
            - CONSTANT_Fieldref_info（字段的符号引用）
            - CONSTANT_Methodref_info（类中方法的符号引用）
            - CONSTANT_InterfaceMethodref_info（接口中方法的符号引用）
            - CONSTANT_String_info（字符串字面量）
            - CONSTANT_Integer_info（整数字面量）
            - CONSTANT_Float_info（浮点数字面量）
            - CONSTANT_Long_info（长整型字面量）
            - CONSTANT_Double_info（双精度浮点数字面量）
            - CONSTANT_NameAndType_info（字段或方法的名称和描述符）
            - CONSTANT_Utf8_info（UTF-8编码的字符串）
            - CONSTANT_MethodHandle_info（方法句柄）
            - CONSTANT_MethodType_info（方法类型）
            - CONSTANT_InvokeDynamic_info（动态调用点）
        - 共同点为第一位是一个u1类型的tag标志位，表示该常量池项的类型
        - 不同点为后续的结构不同，表示不同类型的常量池项所需的信息也不同
- 访问标志：access_flag,类或接口的访问权限和属性的标志位集合
    - 常见的访问标志：
        - ACC_PUBLIC：公共类或接口
        - ACC_FINAL：不可继承的类或不可重写的方法
        - ACC_SUPER：使用新的方法调用语义（JDK 1.0引入）
        - ACC_INTERFACE：接口
        - ACC_ABSTRACT：抽象类或接口
        - ACC_SYNTHETIC：编译器生成的类、方法或字段
        - ACC_ANNOTATION：注解类型
        - ACC_ENUM：枚举类型
- 类索引，父类索引，接口索引集合
- 字段表集合：包括类级变量以及实例级变量
    - 例如字段的标识符，访问修饰符（public,private或protected）,是类变量还是实例变量（static修饰符），是否是常量（final修饰符）
    - 注意：
        - 字段表集合中不会列出从父类或者实现的接口中继承而来的字段，但有可能列出原本java代码中不存在的字段。
        - 在java语言中字段是无法重载的，两个字段的数据类型、修饰符不管是否相同，都必须使用不一样的名称，但是对于字节码来讲，如果两个字段的描述符不一致，那字段重名就是合法的。
    - 字段计数器
    - 字段表
      每个成员都必须是一个fields_info结构的数据项
      字段表作为一个表，同样有自己的结构：
        - 访问标志
        - 字段名索引
        - 字段描述符索引
        - 属性计数器
        - 属性集合：一个字段还可能拥有一些属性，用于存储更多的额外信息。比如初始化值，一些注释信息等。
            - 属性名索引
            - 属性长度
            - 常量值索引（可选）：如果字段是用final修饰的，并且在声明时赋值了一个编译时常量，那么这个属性就会存在
- 方法表集合
    - 注意：
      方法表集合中不会列出从父类或者实现的接口中继承而来的方法，但有可能列出原本java代码中不存在的方法，比如类的构造方法<init>
      和类的静态初始化方法<clinit>。
      在java语言中方法是可以重载的，允许在同一个类中定义多个名称相同但参数列表不同的方法。但是对于字节码来讲，允许存放多个方法前面相同的方法，唯一的条件就是这些方法之间的返回值不能相同
    - 方法计数器
    - 方法表
      每个方法都必须是一个method_info结构的数据项
      方法表作为一个表，同样有自己的结构：
    - 访问标志：多个选项的组合
    - 方法名索引
    - 方法描述符索引
    - 属性计数器
    - 属性集合：方法的属性中最重要的就是Code属性，它存储了方法的字节码指令，还有一些其他的信息，比如局部变量表，操作数栈的深度等。
    - 属性名索引
    - 属性长度
    - 最大栈深度
    - 最大局部变量表大小
    - 字节码长度
    - 字节码指令
    - 异常表长度
    - 异常表
    - 属性计数器
- 属性表集合
  - 指的是class文件所携带的辅助信息，比如该class文件的源文件名，调试信息，注解信息等
  - java8中定义了23种属性类型

# 第2章--字节码指令集与解析举例

# 第3章--类的加载过程详解

# 第4章--再谈类的加载器